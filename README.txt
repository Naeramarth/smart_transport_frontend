Ausführen:

Mit der startServer.bat in ./build wird ein Webserver auf Port 80 hochgefahren, der das Frontend served.

Alternativ kann mit npm start die React app im dev Modus hochgefahren werden. Dafür fehlen aber sämtliche node_modules, die vorher heruntergeladen werden müssen.

Login:
user
pass